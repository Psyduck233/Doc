PROMPT = """
将用户给出的电子器件的参数json数据进行格式清洗，
请严格按照以下json格式输出，不要输出额外的说明，如果参数有条件要求，请将条件写在参数名称的键名中：

{{
  "Device name": {{ 
    "name": "器件名称"
  }}, 
  "Parameters": {{ 
    "参数名称1": "参数值1",
    "参数名称2": "参数值2",
    ...
  }}
}}

1. 对于带有单位的值（如“mΩ”，“V”，“A”等），应保留单位，并确保数据是完整的。
2. 清洗任何不规则的符号，如 “--”，“ - ”等，确保输出数据符合一致的格式。
3. 如果参数键值对的键值为空或是不明意义的键值，请直接删去这个键值对。
4. 对于嵌套的键值对，合并键名，使用下划线连接，并保留数值和单位，例如：
   - "Thermal resistance, junction-case (R_thJC)": {{"Max.": "1.28", "Unit": "°C/W"}}  → "Thermal resistance, junction-case (R_thJC)_Max": "1.28°C/W"
   - 对于这种情况，将 条件（Max.）移动到键名, 单位（Unit）移动到键名，构建新的键值对。
   - 如果某个键值中是多格条件的数组形式，请拆分不同条件为单个键值对。
5. 对于数组值，尝试将其中的数值范围合并成一个字符串，确保格式如下：
   - "V_DS_max": ["650 V", "- -650 V"] → "V_DS_max": "650 V to -650 V"
   - 任何类似格式的数值范围应当合并为 "最小值 to 最大值" 格式。
6. 出现描述性信息，也放置在键名中。

这是用户提供的电子器件json数据：
{json_data}
"""
